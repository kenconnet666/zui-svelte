// 自动生成，请运行 pnpm generate；勿手工修改。来源：csstype 3.2.3（MIT）及 schema.ts。
export interface UnitMethods<Args extends number[]> {
  length: {
    /** 以 px 写入；参数数量与顺序由属性决定。 */
    px(...values: Args): void;
    /** 以 rem 写入；参数数量与顺序由属性决定。 */
    rem(...values: Args): void;
    /** 以 em 写入；参数数量与顺序由属性决定。 */
    em(...values: Args): void;
    /** 以 ch 写入；参数数量与顺序由属性决定。 */
    ch(...values: Args): void;
    /** 以 ex 写入；参数数量与顺序由属性决定。 */
    ex(...values: Args): void;
    /** 以 cap 写入；参数数量与顺序由属性决定。 */
    cap(...values: Args): void;
    /** 以 ic 写入；参数数量与顺序由属性决定。 */
    ic(...values: Args): void;
    /** 以 lh 写入；参数数量与顺序由属性决定。 */
    lh(...values: Args): void;
    /** 以 rlh 写入；参数数量与顺序由属性决定。 */
    rlh(...values: Args): void;
    /** 以 cm 写入；参数数量与顺序由属性决定。 */
    cm(...values: Args): void;
    /** 以 mm 写入；参数数量与顺序由属性决定。 */
    mm(...values: Args): void;
    /** 以 Q 写入；参数数量与顺序由属性决定。 */
    Q(...values: Args): void;
    /** 以 in 写入；参数数量与顺序由属性决定。 */
    in(...values: Args): void;
    /** 以 pt 写入；参数数量与顺序由属性决定。 */
    pt(...values: Args): void;
    /** 以 pc 写入；参数数量与顺序由属性决定。 */
    pc(...values: Args): void;
    /** 以 vw 写入；参数数量与顺序由属性决定。 */
    vw(...values: Args): void;
    /** 以 vh 写入；参数数量与顺序由属性决定。 */
    vh(...values: Args): void;
    /** 以 vi 写入；参数数量与顺序由属性决定。 */
    vi(...values: Args): void;
    /** 以 vb 写入；参数数量与顺序由属性决定。 */
    vb(...values: Args): void;
    /** 以 vmin 写入；参数数量与顺序由属性决定。 */
    vmin(...values: Args): void;
    /** 以 vmax 写入；参数数量与顺序由属性决定。 */
    vmax(...values: Args): void;
    /** 以 svw 写入；参数数量与顺序由属性决定。 */
    svw(...values: Args): void;
    /** 以 svh 写入；参数数量与顺序由属性决定。 */
    svh(...values: Args): void;
    /** 以 svi 写入；参数数量与顺序由属性决定。 */
    svi(...values: Args): void;
    /** 以 svb 写入；参数数量与顺序由属性决定。 */
    svb(...values: Args): void;
    /** 以 lvw 写入；参数数量与顺序由属性决定。 */
    lvw(...values: Args): void;
    /** 以 lvh 写入；参数数量与顺序由属性决定。 */
    lvh(...values: Args): void;
    /** 以 lvi 写入；参数数量与顺序由属性决定。 */
    lvi(...values: Args): void;
    /** 以 lvb 写入；参数数量与顺序由属性决定。 */
    lvb(...values: Args): void;
    /** 以 dvw 写入；参数数量与顺序由属性决定。 */
    dvw(...values: Args): void;
    /** 以 dvh 写入；参数数量与顺序由属性决定。 */
    dvh(...values: Args): void;
    /** 以 dvi 写入；参数数量与顺序由属性决定。 */
    dvi(...values: Args): void;
    /** 以 dvb 写入；参数数量与顺序由属性决定。 */
    dvb(...values: Args): void;
    /** 以 cqw 写入；参数数量与顺序由属性决定。 */
    cqw(...values: Args): void;
    /** 以 cqh 写入；参数数量与顺序由属性决定。 */
    cqh(...values: Args): void;
    /** 以 cqi 写入；参数数量与顺序由属性决定。 */
    cqi(...values: Args): void;
    /** 以 cqb 写入；参数数量与顺序由属性决定。 */
    cqb(...values: Args): void;
    /** 以 cqmin 写入；参数数量与顺序由属性决定。 */
    cqmin(...values: Args): void;
    /** 以 cqmax 写入；参数数量与顺序由属性决定。 */
    cqmax(...values: Args): void;
    /** 以 % 写入；参数数量与顺序由属性决定。 */
    pct(...values: Args): void;
  };
  time: {
    /** 以 ms 写入；参数数量与顺序由属性决定。 */
    ms(...values: Args): void;
    /** 以 s 写入；参数数量与顺序由属性决定。 */
    s(...values: Args): void;
  };
  angle: {
    /** 以 deg 写入；参数数量与顺序由属性决定。 */
    deg(...values: Args): void;
    /** 以 rad 写入；参数数量与顺序由属性决定。 */
    rad(...values: Args): void;
    /** 以 grad 写入；参数数量与顺序由属性决定。 */
    grad(...values: Args): void;
    /** 以 turn 写入；参数数量与顺序由属性决定。 */
    turn(...values: Args): void;
  };
}
